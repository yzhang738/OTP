using System;
namespace OTP.Ring.Business
{
    public interface ICommentsBL
    {
        string GetComments(string tableName, string primaryKey);
        void UpdateComments(string tableName, string primaryKey, string comments);
    }
}
